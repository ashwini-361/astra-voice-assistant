# Deployment

Reserved for Phase C of `docs/roadmap/ASTRA_WEB_SERVICE_PLAN.md` (AWS
deployment). Deliberately empty until Phase A/B (repo hardening,
architecture freeze) are done -- see the roadmap doc's guiding principle:
deploy and instrument only after the application itself has stopped
changing week to week.
