# Agent instructions

See [`CLAUDE.md`](CLAUDE.md) -- the same instructions apply regardless of
which coding agent is reading this repo (code-review-graph MCP usage,
project snapshot, commit conventions). Kept as a separate file only
because Gemini looks for `GEMINI.md` specifically by convention.
